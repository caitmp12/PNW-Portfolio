import React from "react"
import { LInk } from "react-router-dom"

const Nav = (props) => {
    return (
        <div> 
            <h1>This is the Nav!</h1>
        </div>
    )
}

export default Nav;