import React from "react"
import { LInk } from "react-router-dom"

const Gallery = (props) => {
    return (
        <div>
            <h1>Comming Soon</h1>
        </div>
    )
}

export default Gallery;